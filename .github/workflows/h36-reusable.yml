name: H36 Reusable Test
on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string

jobs:
  real:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Real! Input: ${{ inputs.name }}"
